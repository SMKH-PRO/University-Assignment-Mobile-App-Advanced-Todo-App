import { MD3LightTheme } from 'react-native-paper';

const theme = {
  ...MD3LightTheme,
  colors: {
    ...MD3LightTheme.colors,
    primary: '#45AAF2',
    accent: '#f1c40f',
    icon: '#ABABAB',
  },
};

export default theme; 